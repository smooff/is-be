package sk.stuba.sdg.isbe.domain.enums;

public enum JobStatusEnum {
    JOB_FREE,
    JOB_IDLE,
    JOB_PENDING,
    JOB_PROCESSING,
    JOB_DONE,
    JOB_ERR,
    JOB_PAUSED,
    JOB_CANCELED,
    JOB_STATUS_MAX
}
